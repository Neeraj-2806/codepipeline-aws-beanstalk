version: 0.2

phases:
  install:
    commands:
      - echo "Install phase: nothing to do"

  build:
    commands:
      - echo "Build phase started on $(date)"

artifacts:
  files:
    - '**/*'
  exclude-paths:
    - .git/**/*
